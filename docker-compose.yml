services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3001:3000"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
